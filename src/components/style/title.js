import styled from "styled-components"

export default Title = styled.div`
  font-size: 1.5em;
  text-align: center;
  font-family: 'Open-sans', sans-serif;
  color: #262340;
  line-height: 1.55;
  font-weight: normal;
  word-wrap: break-word;
  font-kerning: normal;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
`
